# Copy this file to `.env` for local development.

# PostgreSQL connection string (required for Vercel / production)
# Create a free database at https://neon.tech or use Vercel Postgres
DATABASE_URL="postgresql://user:password@host:5432/dpp_pim?sslmode=require"

# Optional: skip env validation during Docker/CI builds
# SKIP_ENV_VALIDATION=1
